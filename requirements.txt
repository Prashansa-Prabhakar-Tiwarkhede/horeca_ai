# Smart HORECA AI — Python dependencies
# Install: pip install -r requirements.txt

flask>=2.3.0          # Web framework (includes Werkzeug)
scikit-learn>=1.3.0   # ML models
pandas>=2.0.0         # Data processing
numpy>=1.24.0         # Numerical computing
joblib>=1.3.0         # Model serialization
gunicorn>=21.2.0      # Production WSGI server (deployment)

# Note: flask-sqlalchemy and flask-login NOT required.
# This app uses built-in sqlite3 + session-based auth.
